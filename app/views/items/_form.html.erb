<%= form_for @item do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  
  <%= f.label :number %>
  <%= f.number_field :number %>
  <div class="actions">
    <%= f.submit 'Save' %>
  </div>
<% end %>